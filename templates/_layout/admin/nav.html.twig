<header class="flex flex-wrap sm:justify-start sm:flex-nowrap z-50 w-full bg-teal-500 border-b border-white/[.5] text-sm py-3 sm:py-0">
    <nav class="container mx-auto sm:flex px-4 sm:px-6 lg:px-8">
        <div class="flex items-center justify-between">
            <a class="flex-none text-xl font-semibold text-white" href="{{ path('admin_dashboard') }}"
               aria-label="Brand">
                <img src="{{ asset('assets/images/logo.png') }}" class="h-14"/>
            </a>
            <div class="sm:hidden">

            </div>
        </div>
        <div id="navbar-collapse-with-animation"
             class="hs-collapse hidden overflow-hidden transition-all duration-300 basis-full grow sm:block">
            <div class="flex flex-col gap-y-4 gap-x-0 mt-5 sm:flex-row sm:items-center sm:justify-end sm:gap-y-0 sm:gap-x-7 sm:mt-0 sm:pl-7">

            </div>
        </div>
    </nav>
</header>