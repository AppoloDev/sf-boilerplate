<div class="max-w-sm w-full min-h-[200px] flex flex-col justify-center mx-auto px-6 py-4">
    <h2 class="mt-5 font-semibold text-gray-800">
        Aucun résultat.
    </h2>
    <p class="mt-2 text-sm text-gray-600">
        {{ text }}
    </p>

    {% if buttonLink is not null %}
        <div class="mt-5 grid sm:flex gap-2">
            <a href="{{ buttonLink }}"
               class="py-2 px-3 inline-flex justify-center items-center gap-2 rounded-md border border-transparent font-semibold bg-teal-500 text-white hover:bg-teal-600 focus:outline-none focus:ring-2 focus:ring-teal-500 focus:ring-offset-2 transition-all text-sm">
                <svg class="w-3 h-3" xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 16 16"
                     fill="none">
                    <path d="M2.63452 7.50001L13.6345 7.5M8.13452 13V2" stroke="currentColor" stroke-width="2"
                          stroke-linecap="round"></path>
                </svg>
                {{ buttonText }}
            </a>
        </div>
    {% endif %}
</div>